define(['base-view'], function(BaseView) {
  'use strict';

  return BaseView.extend({
    className: 'server-list',

    template: _.template('Server List'),

    initialize: function () {

    }
  });

});
